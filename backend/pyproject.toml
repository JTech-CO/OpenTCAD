[project]
name = "opentcad-backend-contract"
version = "0.1.0"
description = "Gated runtime and broker contract foundation for OpenTCAD"
requires-python = ">=3.12,<3.15"
dependencies = []

[tool.unittest]
start-directory = "tests"
